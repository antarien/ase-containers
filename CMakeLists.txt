# Stub CMakeLists.txt for ase-containers
# TODO: Implement actual build logic

cmake_minimum_required(VERSION 3.20)
project(ase-containers VERSION 0.1.0 LANGUAGES CXX)

message(STATUS "Stub: ase-containers (not implemented yet)")

# Placeholder target
add_custom_target(ase-containers_stub ALL
    COMMAND ${CMAKE_COMMAND} -E echo "Module ase-containers is a stub"
)
